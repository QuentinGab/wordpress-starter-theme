<?php

/**
 * Register all your shortcodes here
 * 
 * Here is an example
 * add_shortcode('shortcode_name', function ($atts) {
    $a = shortcode_atts(array(
        'id' => '',
    ), $atts);


    return view("/resources/components/shortcode_name", $a);
  });
 */
